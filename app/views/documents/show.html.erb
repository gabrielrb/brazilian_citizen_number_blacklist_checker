<div class="container">
  <%= simple_form_for :search, url: root_path, method: "GET" do |f| %>
    <%= f.input :number, input_html: {value: ""} %>
    <%= f.submit "Search", class: "btn btn-primary" %>
  <% end %>
  <% unless @document.nil? %>
    <div class="col-xs-12 col-sm-8 offset-sm-2">
      <h1>CPF: <%= @document.number %></h1>
    </div>
  <% end %>
</div>

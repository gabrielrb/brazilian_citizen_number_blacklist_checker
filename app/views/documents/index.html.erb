<div class="container">
  <div class="mt-4">
    <h1 class="d-flex justify-content-center">"CPF" Blacklist</h1>

    <h3 class="border-bottom mt-5">This is a database created to blacklist CPF numbers of those who have committed violations against the company's statute.</h3>

    <h3 class="mt-4">What is a CPF?</h3>
    <p class="mt-4 border-bottom">
      "CPF" stands for <em>"Cadastro de Pessoas Físicas"</em> (Natural Persons Register).
      A CPF is an individual taxpayer identification number given to people living in Brazil,
      both native Brazilians and resident aliens, who pay taxes.
    </p>
  </div>
  <div class="row">
    <div class="mt-4 col-6">
      <%= simple_form_for :search, url: root_path, input_html: {id: :form}, method: "GET" do |f| %>
        <%= f.input :number, input_html: {value: "", id: :cpf}, label: "CPF:", placeholder: "Type here the document number" %>
        <div class="errors">
          <ul class="errors-list">
          </ul>
        </div>
        <%= f.submit "Search on the blacklist", id: "search-button", class: "btn btn-warning rounded-pill" %>
      <% end %>
    </div>
    <% if @document.present? %>
      <div class="mt-4 col-6">
        <div class="row">
          <div class="col-6">
            <h5>CPF: <%= @document_number %></h5>
            <h5>BLOCK</h5>
          </div>
          <div class="col-6">
            <%= link_to "Remove from blacklist", document_path(@document), method: :delete, remote: true, data: { confirm: 'Are you sure?' }, class: "btn btn-warning rounded-pill" %>
          </div>
        </div>
      </div>
    <% end %>
    <% if @document.nil? && @document_number.present? %>
      <div class="mt-4 col-6">
        <h3>CPF: <%= @document_number %> - FREE</h3>
        <%= simple_form_for (@new_document), remote: true do |f| %>
          <%= f.input :number, as: :hidden, input_html: { value: @document_number } %>
          <%= f.submit 'Add to the blacklist', class: 'btn btn-warning rounded-pill' %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
